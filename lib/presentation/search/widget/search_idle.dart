import 'package:flutter/material.dart';

class StateIdle extends StatelessWidget {
  const StateIdle({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(child: Container());
  }
}
